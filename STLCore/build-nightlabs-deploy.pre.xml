<project>
	
	<target name="deploy.pre">
		<copy todir="${j2ee.deploy.dir}/${j2ee.deploy.dir.lib}">
			<fileset dir="${stl.deploy.lib}" includes="${stl.deploy.lib.includes}"/>
		</copy>
	</target>
	
</project>